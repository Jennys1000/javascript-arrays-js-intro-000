var chocolateBars = ['snickers', 'hundred grand', 'kitkat', 'skittles']
function addElementToBeginningOfArray(chocolateBars, reeses) {
return [reeses, ...chocolateBars]
}
function addElementToBeginningOfArray(chocolateBars, reeses) { 
return [chocolatebar.unshift(reeses)]
}